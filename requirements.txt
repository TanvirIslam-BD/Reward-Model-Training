# Pinned to match the Skills Network "Reward Modeling" lab for reproducibility.
# torch is installed separately (CPU or CUDA) — see README.
datasets==3.2.0
trl==0.11
huggingface_hub==0.36.0
transformers==4.43.4
peft==0.14.0
nltk==3.9.1
rouge_score==0.1.2
matplotlib==3.10.0
rich==13.7.1
# bitsandbytes is GPU-only (8-bit optimizers/quantization). Optional — uncomment
# only on a CUDA machine; it is NOT imported by this project.
# bitsandbytes==0.43.1
